{
  "name": "door-lock-cloudflare-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "session:update": "node update-session.js",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "@cloudflare/playwright": "^0.0.10",
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
