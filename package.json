{
  "name": "door-lock-cloudflare-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "local:test": "node local-test.js",
    "local:test:prod": "SESSION_IMPORT_URL=https://door-lock-monitor.irvinfly.workers.dev/import-session node local-test.js"
  },
  "dependencies": {
    "@cloudflare/playwright": "^0.0.10",
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
